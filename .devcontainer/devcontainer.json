{
  "name": "NEXUS Agent Cloud",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python"
      ]
    }
  },
  "onCreateCommand": "curl -fsSL https://ollama.com/install.sh | sh",
  "postCreateCommand": "pip3 install aiohttp beautifulsoup4 lxml rich && (ollama serve &) && sleep 8 && ollama pull dolphin3 && echo '=== NEXUS READY ===' && ollama list && free -h"
}
